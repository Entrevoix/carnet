# Session handoff — 2026-07-04 (Stage 2 B3 shipped, B0 in flight)

## State at handoff

Stage 2 execution has started. Design branch `docs/fable-audit-and-design` (PR #62) is complete and merged-pending-review — six design docs, adversarial-reviewed, no app code. Implementation branches are branching from `main`.

## What shipped this session

### B3 — LLM markdown sanitizer + frontmatter normalizer (PR #63, branch `feat/b3-sanitize-normalize`)

- **New:** `apps/mobile/src/lib/enrichSanitize.ts` — pure sanitizer (neutralize-not-delete) + frontmatter normalizer
- **Wire-up:** `omniroute.ts` at the `executeChat` chokepoint — covers every capture mode at once
- **Tests:** 623/623 green (600 pre-existing + 23 new); `tsc --noEmit` clean; committed `8ca48c0`
- **Closes:** security H1 (Obsidian code-execution via LLM output), L6 (external-image beaconing falls out)
- **Known design note:** null-on-bad-frontmatter falls back to neutralized-but-non-canonical rather than surfacing a degraded banner — because existing omniroute fixtures feed minimal frontmatter and enrich fns are non-nullable. Neutralization (the actual security gate) is unconditional. The harder propagation (banner on bad frontmatter) should be wired in B4 when caller null-handling is already being touched.

### B0 — network-control hardening (IN FLIGHT, Opus executor running)

- Branch: `feat/b0-net-hardening` from `main`
- **M3 fix:** `omniroute.ts:181` + `karakeep.ts:105` — replace right-unanchored prefix regex with `new URL()` exact host matching; adds `192.168.*` to LAN allowlist (original regex missed it)
- **M2 fix:** `urlpreview.ts` — `redirect: "manual"`, re-check `isBlockedHost` on each hop's `Location` header, 5-hop limit
- When the executor notifies: check results, push branch, open PR vs `main`

## Stage 2 branch sequence (updated)

```
B3 ✓ (PR #63 open)
B0 → in flight
B1 → next after B0 merges (model split: chatModel/visionModel)
B2 → gated on OCR quality test (do side-by-side first)
B4 → capture timing (save-first for Idea; includes net-new conflict detection)
B5 → notification inline reply (depends on B4)
B6 → browse/search Phase 1 (independent, any time)
B7 → on-device backend Phase 1 (depends on B1)
```

## Open questions — answers needed before some branches

| # | Question | Status |
|---|---|---|
| 1 | OmniRoute at 192.168.x LAN? (informs B0 M3 messaging) | **Unanswered** |
| 2 | Cost tolerance / OmniRoute spend? (gates B7 priority) | **Unanswered** |
| 3 | Vision policy: client-side split (recommended) vs server-side? | **Confirm client-side** |
| 4 | `/ocr` VLM quality bar — do the side-by-side on real cards | **Unanswered — hardware test needed** |
| 5 | Save-first default on (recommended) vs opt-in? | **Confirm default on** |
| 6 | Vestigial `omniRouteTranscriptionModel`: repurpose as visionModel in B1 | **Confirm repurpose** |
| 7 | Quick Settings tile: drop from roadmap (notification covers it) | **Confirm drop** |

Questions 1, 2, 4 need user input. 3, 5, 6, 7 are confirm-if-no-objection.

## Key design docs (all in `.claude/PRPs/`)

| Doc | Purpose |
|---|---|
| `plans/stage2-backend-and-capture.plan.md` | Master B0–B7 plan with dependency graph |
| `prds/capture-timing.decision.md` | B4/B5 save-first mechanics (read before coding B4) |
| `reviews/security-trust-boundaries-2026-07-04.md` | H1/M2/M3/M4 security findings with file:line evidence |
| `prds/v0.5-browse-search.prd.md` | B6 browse/search Phase 1 spec |
| `prds/on-device-backend.prd.md` | B7 on-device backend Phase 1 spec |
| `prds/v0.4-ai-deepening.prd.md` | S1–S4 AI deepening (parallel axis, not in B sequence) |

## Critical constraints (non-negotiable)

- **No SQLite** — expo-sqlite@55 ABI-broken on SDK 54; all persistence via AsyncStorage
- **CI gates:** `vitest run` (600+ tests) + `tsc --noEmit` only — no lint script
- **Frontmatter conventions** must stay byte-compatible (verified in AUDIT.md §1.5)
- **Attribution disabled** in commits (configured globally)
- Branch from `main`, one PR per branch, TDD

## v0.4 AI deepening (separate axis)

Slates S1–S4 are independent of the B branches. S3 reuses `getTagIndex()` (already exists in `vault.ts:65`). S4 uses AsyncStorage base64-packed Float32Array (~120KB for 20-recents), not SQLite. Can be started any time alongside B-branch work.
